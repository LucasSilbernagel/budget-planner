# Database Configuration
# For development, use a local PostgreSQL instance
# For production, use Scaleway PostgreSQL (EU region only)

DATABASE_URL=postgresql://user:password@localhost:5432/budget_planner

# Scaleway Production Database (uncomment when deploying)
# SCALEWAY_DATABASE_URL=postgresql://user:password@host:port/database
